import {NewsletterConfirm} from '@/components/newsletter-confirm';export const metadata={title:'Newsletter',referrer:'no-referrer' as const,robots:{index:false,follow:false}};export default function Page({searchParams}:{searchParams:{token?:string;unsubscribe?:string}}){return <main className="access"><span className="eyebrow">THE WATCHTOWER LETTER</span><h1>{searchParams.unsubscribe?'See you around.':'One last step.'}</h1>{searchParams.token?<NewsletterConfirm token={searchParams.token} unsubscribe={searchParams.unsubscribe==='1'}/>:<p>Open the link in your newsletter email to manage your subscription.</p>}</main>;}
