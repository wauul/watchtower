'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
export function AccountForm({name}:{name:string}){const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);const router=useRouter();return <form onSubmit={async e=>{e.preventDefault();const data=new FormData(e.currentTarget);setBusy(true);try{const r=await fetch('/api/account',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({displayName:data.get('displayName')})});if(!r.ok)throw new Error();setMessage('Profile saved.');router.refresh();}catch{setMessage('Could not save your profile.');}finally{setBusy(false);}}}><label htmlFor="displayName">Public display name</label><input id="displayName" name="displayName" defaultValue={name} minLength={2} maxLength={50} required/><p className="subtle">Shown beside finds you share. Your email is never shown on the public feed.</p><button className="primary" disabled={busy}>{busy?'Saving…':'Save profile'}</button><p role="status">{message}</p></form>;}
