'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {ConfirmButton} from './confirm-button';
export function BoughtButton({id,bought}:{id:string;bought:boolean}){const [busy,setBusy]=useState(false),[error,setError]=useState('');const router=useRouter();async function save(){setBusy(true);setError('');try{const r=await fetch(`/api/products/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({bought:!bought})});if(!r.ok)throw new Error();router.refresh();}catch{setError('Could not update purchase status.');}finally{setBusy(false);}}return <div><ConfirmButton disabled={busy} title={bought?'Undo this purchase?':'Mark this product as bought?'} description={bought?'This removes the purchase from your savings total and resumes tracking.':'This saves the current observed price as your purchase price and pauses further checks. You can undo it later.'} onConfirm={save}>{busy?'Saving…':bought?'Undo purchase':'Mark as bought'}</ConfirmButton>{error&&<p role="alert">{error}</p>}</div>;}
