'use client';
import { ResponsiveContainer,AreaChart,Area,XAxis,YAxis,Tooltip,CartesianGrid,ReferenceLine } from 'recharts';
export function PriceChart({points,currency,compact=false,target}:{points:{price:number;date:string}[];currency:string;compact?:boolean;target?:number|null}){const low=points.length>1&&points.at(-1)!.price<Math.min(...points.slice(0,-1).map(p=>p.price));return <div className={low?'chart new-low':'chart'} style={{height:compact?65:290}} aria-label={`Price history: ${points.length} observations`}><ResponsiveContainer width="100%" height="100%"><AreaChart data={points} margin={{top:10,right:compact?0:16,left:0,bottom:0}}><defs><linearGradient id={compact?'spark-fill':'chart-fill'} x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#4f7d58" stopOpacity={.2}/><stop offset="100%" stopColor="#4f7d58" stopOpacity={0}/></linearGradient></defs>{!compact&&<><CartesianGrid vertical={false} stroke="#e9ede7"/><XAxis dataKey="date" tickFormatter={s=>new Date(s).toLocaleDateString(undefined,{month:'short',day:'numeric'})} axisLine={false} tickLine={false}/><YAxis domain={['auto','auto']} axisLine={false} tickLine={false} width={65}/><Tooltip labelFormatter={s=>new Date(s).toLocaleString()} formatter={(v:number)=>[new Intl.NumberFormat(undefined,{style:'currency',currency}).format(v),'Price']}/>{target&&<ReferenceLine y={target} stroke="#bb8742" strokeDasharray="4 4" label="Target"/>}</>}<Area type="monotone" dataKey="price" stroke="#4f7d58" strokeWidth={2} fill={`url(#${compact?'spark-fill':'chart-fill'})`} dot={points.length===1} isAnimationActive={false}/></AreaChart></ResponsiveContainer></div>;}
