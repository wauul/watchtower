'use client';
import { FormEvent,useState } from 'react';
import { ArrowRight,Link2,LoaderCircle,CheckCircle2 } from 'lucide-react';
export function TrackForm(){const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');const [ok,setOk]=useState(false);
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);setMessage('');try{const res=await fetch('/api/products',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url:f.get('url'),email:f.get('email'),country:f.get('country'),targetPrice:f.get('target')?Number(f.get('target')):null})});const data=await res.json();setOk(res.ok);setMessage(data.message||data.error);}catch{setOk(false);setMessage('Could not reach Watchtower. Please try again.');}finally{setBusy(false);}}
 return <form onSubmit={submit} className="track-form"><div className="form-heading"><span className="eyebrow">YOUR NEXT GOOD BUY</span><span className="step">01 / SET YOUR WATCH</span></div><h2>Got your eye on something?</h2><p>Drop the link. We’ll keep watch.</p><label htmlFor="url">Product link</label><div className="input-icon"><Link2 size={18}/><input id="url" name="url" type="url" placeholder="https://store.com/that-thing-you-want" required maxLength={2000}/></div><div className="form-grid"><div><label htmlFor="target">Target price <span>optional</span></label><input id="target" name="target" type="number" min="0.01" step="0.01" placeholder="Your happy price"/><small>In the listing’s currency</small></div><div><label htmlFor="country">Shopping from</label><select id="country" name="country" defaultValue="Nigeria"><option>Nigeria</option><option>France</option><option>United States</option><option>United Kingdom</option><option>Germany</option><option>Canada</option><option>India</option><option>Australia</option><option>South Africa</option></select></div></div><label htmlFor="email">Your email</label><input id="email" name="email" type="email" placeholder="you@example.com" required/><small>Your private dashboard link and the deals worth your time.</small><button className="primary" disabled={busy}>{busy?<><LoaderCircle className="spin" size={18}/> Taking a first look…</>:<>Start watching <ArrowRight size={18}/></>}</button><div className="form-note"><CheckCircle2 size={15}/> No password. No noisy alerts. Just the good stuff.</div>{message&&<p role="status" className={ok?'notice':'error'}>{message}</p>}</form>;
}
export function AccessForm(){const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const email=new FormData(e.currentTarget).get('email');setBusy(true);try{const r=await fetch('/api/access',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setMessage(d.message||d.error);}catch{setMessage('Could not send your link. Try again.');}finally{setBusy(false);}}return <form onSubmit={submit} className="access-form"><label htmlFor="access-email">Your email</label><input id="access-email" type="email" name="email" required placeholder="you@example.com"/><button className="primary" disabled={busy}>{busy?'Sending…':'Email my dashboard link'}<ArrowRight size={18}/></button>{message&&<p role="status" className="notice">{message}</p>}</form>;}
