'use client';import {useState} from 'react';import {ArrowUpRight,CheckCircle2,LoaderCircle} from 'lucide-react';
export function Newsletter(){const [busy,setBusy]=useState(false),[success,setSuccess]=useState(false),[error,setError]=useState('');return <section className="newsletter"><div><span className="eyebrow">THE OCCASIONAL GOOD THING</span><h2>A better inbox.<br/><em>Not a busier one.</em></h2><p>Watchtower news and selected finds, when there’s something worth sharing.</p></div>{success?<div className="newsletter-success" role="status"><CheckCircle2 size={32}/><h3>Check your inbox.</h3><p>If you’re not already subscribed, we’ve sent a confirmation link. You choose when to join.</p></div>:<form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{const data=new FormData(e.currentTarget);const r=await fetch('/api/newsletter',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:data.get('email')})});const d=await r.json();if(!r.ok)throw new Error(d.error);setSuccess(true);}catch(e){setError(e instanceof Error?e.message:'Please try again.');}finally{setBusy(false);}}}><label htmlFor="newsletter-email">Your email address</label><div className="newsletter-input"><input id="newsletter-email" name="email" type="email" required maxLength={254} placeholder="you@example.com"/><button className="primary" disabled={busy}>{busy?<LoaderCircle className="spin" size={18}/>:<>Count me in <ArrowUpRight size={17}/></>}</button></div><p className="subtle">By signing up, you request newsletter emails. Confirm by email; unsubscribe whenever you like.</p>{error&&<p role="alert" className="error">{error}</p>}</form>}</section>;}
